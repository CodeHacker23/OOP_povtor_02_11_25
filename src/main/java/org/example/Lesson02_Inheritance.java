package org.example;

/**
 * ============================================================================
 * УРОК 2: НАСЛЕДОВАНИЕ (INHERITANCE)
 * ============================================================================
 * 
 * ПРИВЕТ, БУДУЩИЙ ПРОГРАММИСТ! 🚀
 * 
 * Ты уже изучил ИНКАПСУЛЯЦИЮ - первый принцип ООП.
 * Теперь изучаем второй принцип - НАСЛЕДОВАНИЕ.
 * 
 * Это один из самых важных механизмов ООП!
 * 
 * ============================================================================
 * ЧАСТЬ 1: ТЕОРИЯ - ЧТО ТАКОЕ НАСЛЕДОВАНИЕ?
 * ============================================================================
 * 
 * НАСЛЕДОВАНИЕ (Inheritance) - это механизм, при котором один класс
 * (дочерний/child) наследует поля и методы другого класса (родительского/parent).
 * 
 * ПРОСТЫМИ СЛОВАМИ:
 * 
 * Наследование = "IS-A" отношение
 * 
 * Кот - это животное. Собака - это животное.
 * Car IS-A Vehicle. Dog IS-A Animal.
 * 
 * Дочерний класс НАСЛЕДУЕТ всё от родительского и может добавить своё.
 * 
 * ============================================================================
 * ЧАСТЬ 2: АНАЛОГИИ ИЗ ЖИЗНИ
 * ============================================================================
 * 
 * АНАЛОГИЯ 1: СЕМЕЙНОЕ ДЕРЕВО
 * 
 * Представь семейное дерево:
 * 
 *          ЧЕЛОВЕК (родительский класс)
 *          ├── имя, возраст, ест(), спит()
 *          │
 *          ├── ПРОГРАММИСТ (дочерний класс)
 *          │   └── всё от человека + пишет код()
 *          │
 *          └── ПОВАР (дочерний класс)
 *              └── всё от человека + готовит еду()
 * 
 * Программист - это человек. Повар - это человек.
 * Оба имеют имя, возраст, умеют есть и спать.
 * Но каждый делает что-то особенное!
 * 
 * АНАЛОГИЯ 2: ТРАНСПОРТ
 * 
 *          ТРАНСПОРТНОЕ СРЕДСТВО
 *          ├── марка, год, скорость, start(), stop()
 *          │
 *          ├── МАШИНА
 *          │   └── всё от транспорта + honk() (бибикать)
 *          │
 *          ├── ВЕЛОСИПЕД
 *          │   └── всё от транспорта + ringBell() (звенеть)
 *          │
 *          └── МОТОЦИКЛ
 *              └── всё от транспорта + wheelie() (езда на заднем колесе)
 * 
 * Все транспортные средства могут стартовать и останавливаться.
 * Но каждый делает это по-своему и имеет свои особенности!
 * 
 * АНАЛОГИЯ 3: ЖИВОТНЫЕ
 * 
 *          ЖИВОТНОЕ
 *          ├── имя, голод, eat(), sleep(), makeSound()
 *          │
 *          ├── КОТ
 *          │   └── makeSound() = "Мяу!" + knockThingsOffTable()
 *          │
 *          ├── СОБАКА
 *          │   └── makeSound() = "Гав!" + fetchBall()
 *          │
 *          └── ПТИЦА
 *              └── makeSound() = "Чирик!" + fly()
 * 
 * Все животные издают звуки, но каждый по-своему!
 * 
 * ============================================================================
 * ЧАСТЬ 3: КАК ЭТО РАБОТАЕТ В JAVA
 * ============================================================================
 * 
 * В Java наследование реализуется через ключевое слово extends:
 * 
 * class РодительскийКласс {
 *     // поля и методы
 * }
 * 
 * class ДочернийКласс extends РодительскийКласс {
 *     // наследует всё от родителя
 *     // может добавить свои поля и методы
 *     // может переопределить методы родителя
 * }
 * 
 * КЛЮЧЕВЫЕ ПОНЯТИЯ:
 * 
 * 1. EXTENDS - ключевое слово для наследования
 *    class Cat extends Animal { }
 * 
 * 2. SUPER - ссылка на родительский класс
 *    super.method() - вызывает метод родителя
 *    super.field - обращается к полю родителя
 * 
 * 3. SUPER() - вызов конструктора родителя
 *    В конструкторе дочернего класса ОБЯЗАТЕЛЬНО вызвать super() ПЕРВЫМ!
 * 
 * 4. @Override - аннотация для переопределения метода
 *    Говорит: "Я переопределяю метод родителя"
 * 
 * 5. ПРИВЕДЕНИЕ ТИПОВ - дочерний класс можно использовать как родительский
 *    Animal animal = new Cat(); // Cat IS-A Animal
 * 
 * ============================================================================
 * ЧАСТЬ 4: ЗАЧЕМ ЭТО НУЖНО?
 * ============================================================================
 * 
 * 1. ПЕРЕИСПОЛЬЗОВАНИЕ КОДА (DRY - Don't Repeat Yourself)
 * 
 *    БЕЗ наследования:
 *    class Cat {
 *        String name;
 *        void eat() { ... }
 *        void sleep() { ... }
 *    }
 *    
 *    class Dog {
 *        String name;      // дублирование!
 *        void eat() { ... }  // дублирование!
 *        void sleep() { ... } // дублирование!
 *    }
 * 
 *    С наследованием:
 *    class Animal {
 *        String name;
 *        void eat() { ... }
 *        void sleep() { ... }
 *    }
 *    
 *    class Cat extends Animal { }
 *    class Dog extends Animal { }
 *    
 *    Код написан один раз - используется везде!
 * 
 * 2. ИЕРАРХИЯ КЛАССОВ
 * 
 *    Создаётся логическая структура:
 *    Animal → Cat, Dog, Bird
 *    Vehicle → Car, Bike, Motorcycle
 *    
 *    Легче понять отношения между классами
 * 
 * 3. ПОЛИМОРФИЗМ (следующий урок)
 * 
 *    Можно работать с разными типами через общий интерфейс:
 *    Animal[] animals = {new Cat(), new Dog()};
 *    for (Animal a : animals) {
 *        a.makeSound(); // каждый издаст свой звук!
 *    }
 * 
 * 4. ГИБКОСТЬ
 * 
 *    Можно добавить новый класс, наследуя от существующего:
 *    class Bird extends Animal { }
 *    
 *    Не нужно переписывать общую логику!
 * 
 * ============================================================================
 * ЧАСТЬ 5: ДЕТАЛИ РАБОТЫ
 * ============================================================================
 * 
 * ЧТО НАСЛЕДУЕТСЯ:
 * ✅ Все public и protected поля
 * ✅ Все public и protected методы
 * ✅ Конструкторы НЕ наследуются напрямую
 * 
 * ЧТО НЕ НАСЛЕДУЕТСЯ:
 * ❌ private поля и методы (но можно через getter/setter)
 * ❌ Конструкторы (но можно вызвать через super())
 * ❌ final классы (нельзя наследовать)
 * 
 * КОНСТРУКТОРЫ:
 * 
 * В дочернем классе ОБЯЗАТЕЛЬНО вызвать super() в конструкторе:
 * 
 * class Animal {
 *     private String name;
 *     
 *     public Animal(String name) {
 *         this.name = name;
 *     }
 * }
 * 
 * class Cat extends Animal {
 *     public Cat(String name) {
 *         super(name); // ОБЯЗАТЕЛЬНО! И ПЕРВЫМ!
 *     }
 * }
 * 
 * ПЕРЕОПРЕДЕЛЕНИЕ МЕТОДОВ:
 * 
 * class Animal {
 *     public void makeSound() {
 *         System.out.println("Звук животного");
 *     }
 * }
 * 
 * class Cat extends Animal {
 *     @Override
 *     public void makeSound() {
 *         System.out.println("Мяу!"); // своя реализация
 *     }
 * }
 * 
 * Cat cat = new Cat();
 * cat.makeSound(); // выведет "Мяу!", а не "Звук животного"
 * 
 * ПРИВЕДЕНИЕ ТИПОВ:
 * 
 * Cat cat = new Cat();
 * Animal animal = cat; // Cat IS-A Animal, можно!
 * 
 * Animal animal = new Cat();
 * animal.makeSound(); // вызовет метод Cat!
 * 
 * // Но нельзя вызвать методы, которых нет в Animal:
 * // animal.knockThingsOffTable(); // ОШИБКА! Animal не знает об этом методе
 * 
 * // Можно проверить тип и привести:
 * if (animal instanceof Cat) {
 *     Cat realCat = (Cat) animal; // приведение типа
 *     realCat.knockThingsOffTable(); // теперь можно!
 * }
 * 
 * ============================================================================
 * ЧАСТЬ 6: ПРИМЕРЫ КОДА
 * ============================================================================
 */

// ============================================
// РОДИТЕЛЬСКИЙ КЛАСС (BASE CLASS / SUPERCLASS)
// ============================================
class Vehicle {
    // Общие поля для всех транспортных средств
    private String brand;
    private int year;
    private double speed;
    
    // Конструктор родительского класса
    public Vehicle(String brand, int year) {
        this.brand = brand;
        this.year = year;
        this.speed = 0;
        System.out.println("Создан транспорт: " + brand + " " + year);
    }
    
    // Общие методы для всех транспортных средств
    public void start() {
        System.out.println(brand + " заведен!");
    }
    
    public void stop() {
        speed = 0;
        System.out.println(brand + " остановлен!");
    }
    
    public void accelerate(double amount) {
        speed += amount;
        System.out.println(brand + " разогнался до " + speed + " км/ч");
    }
    
    // Геттеры
    public String getBrand() { return brand; }
    public int getYear() { return year; }
    public double getSpeed() { return speed; }
    
    // Метод, который можно переопределить в дочерних классах
    public String getInfo() {
        return brand + " " + year + " года, скорость: " + speed + " км/ч";
    }
}

// ============================================
// ДОЧЕРНИЙ КЛАСС 1: CAR (НАСЛЕДУЕТ VEHICLE)
// ============================================
class Car extends Vehicle {
    // Уникальные поля только для машины
    private int doorCount;
    private boolean hasSunroof;
    
    // Конструктор - ВСЕГДА вызываем super() первым!
    public Car(String brand, int year, int doorCount, boolean hasSunroof) {
        super(brand, year); // ОБЯЗАТЕЛЬНО! Вызываем конструктор родителя
        this.doorCount = doorCount;
        this.hasSunroof = hasSunroof;
        System.out.println("  Это машина с " + doorCount + " дверями");
    }
    
    // Уникальный метод только для машины
    public void honk() {
        System.out.println("БИИП-БИИП! (машина сигналит)");
    }
    
    // Переопределение метода родителя (@Override)
    @Override
    public String getInfo() {
        // Используем super для вызова метода родителя
        String parentInfo = super.getInfo();
        return parentInfo + ", дверей: " + doorCount 
               + ", люк: " + (hasSunroof ? "есть" : "нет");
    }
    
    // Переопределение accelerate - машина имеет ограничение скорости
    @Override
    public void accelerate(double amount) {
        double currentSpeed = getSpeed();
        double newSpeed = currentSpeed + amount;
        
        if (newSpeed > 200) {
            System.out.println("⚠️ Слишком быстро! Ограничиваю до 200 км/ч");
            amount = 200 - currentSpeed; // ограничиваем ускорение
        }
        
        super.accelerate(amount); // вызываем метод родителя
    }
}

// ============================================
// ДОЧЕРНИЙ КЛАСС 2: BIKE (НАСЛЕДУЕТ VEHICLE)
// ============================================
class Bike extends Vehicle {
    private int wheelCount;
    private boolean isElectric;
    
    public Bike(String brand, int year, int wheelCount, boolean isElectric) {
        super(brand, year); // ОБЯЗАТЕЛЬНО! Вызываем конструктор родителя
        this.wheelCount = wheelCount;
        this.isElectric = isElectric;
        System.out.println("  Это велосипед с " + wheelCount + " колесами");
    }
    
    // Уникальный метод велосипеда
    public void ringBell() {
        System.out.println("ДЗЫНЬ-ДЗЫНЬ! (звонок велосипеда)");
    }
    
    @Override
    public String getInfo() {
        return super.getInfo() + ", колес: " + wheelCount 
               + ", электрический: " + (isElectric ? "да" : "нет");
    }
    
    // Переопределение - велосипед не может разгоняться так же быстро
    @Override
    public void accelerate(double amount) {
        double maxAccel = isElectric ? 10 : 5; // электрический быстрее
        double actualAccel = Math.min(amount, maxAccel);
        super.accelerate(actualAccel);
    }
}

/**
 * ============================================================================
 * ЧАСТЬ 7: ДЕМОНСТРАЦИЯ - ВИДИМ ВСЁ В ДЕЙСТВИИ
 * ============================================================================
 */
public class Lesson02_Inheritance {
    
    /**
     * Демонстрация базового наследования
     */
    public static void demonstrateInheritance() {
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║  ДЕМОНСТРАЦИЯ НАСЛЕДОВАНИЯ                              ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        // Создаем объекты дочерних классов
        Car myCar = new Car("Toyota", 2020, 4, true);
        Bike myBike = new Bike("Giant", 2022, 2, true);
        
        System.out.println("\n--- МАШИНА ---");
        myCar.start();                    // метод из родителя
        myCar.accelerate(50);             // переопределенный метод
        myCar.honk();                     // уникальный метод машины
        System.out.println(myCar.getInfo()); // переопределенный метод
        myCar.stop();                     // метод из родителя
        
        System.out.println("\n--- ВЕЛОСИПЕД ---");
        myBike.start();                   // метод из родителя (тот же!)
        myBike.accelerate(50);            // переопределенный метод (ограничение!)
        myBike.ringBell();                // уникальный метод велосипеда
        System.out.println(myBike.getInfo()); // переопределенный метод
        myBike.stop();                    // метод из родителя
        
        System.out.println("\n═══════════════════════════════════════════════════════════\n");
    }
    
    /**
     * Демонстрация приведения типов
     */
    public static void demonstrateTypeCasting() {
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║  ПРИВЕДЕНИЕ ТИПОВ                                       ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        // Car и Bike - это Vehicle!
        Car car = new Car("BMW", 2021, 4, false);
        Bike bike = new Bike("Trek", 2023, 2, false);
        
        // Можно присвоить дочерний класс переменной родительского типа
        Vehicle vehicle1 = car;   // Car IS-A Vehicle
        Vehicle vehicle2 = bike;  // Bike IS-A Vehicle
        
        System.out.println("--- Машина как Vehicle ---");
        vehicle1.start();
        System.out.println(vehicle1.getInfo()); // вызовет переопределенный метод Car!
        
        System.out.println("\n--- Велосипед как Vehicle ---");
        vehicle2.start();
        System.out.println(vehicle2.getInfo()); // вызовет переопределенный метод Bike!
        
        // Но нельзя вызвать уникальные методы:
        // vehicle1.honk(); // ❌ ОШИБКА! Vehicle не знает о honk()
        
        // Можно проверить тип и привести:
        if (vehicle1 instanceof Car) {
            Car realCar = (Car) vehicle1; // приведение типа
            realCar.honk(); // теперь можно!
        }
        
        System.out.println("\n═══════════════════════════════════════════════════════════\n");
    }
    
    /**
     * Преview полиморфизма (полностью в следующем уроке)
     */
    public static void demonstratePolymorphismPreview() {
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║  ПРЕВЬЮ ПОЛИМОРФИЗМА (полностью в Уроке 3)              ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        // Массив Vehicle, но заполняем разными типами!
        Vehicle[] garage = {
            new Car("BMW", 2021, 4, false),
            new Bike("Trek", 2023, 2, false),
            new Car("Mercedes", 2020, 2, true)
        };
        
        System.out.println("Один цикл обрабатывает разные типы:\n");
        
        // Один цикл обрабатывает разные типы!
        for (Vehicle vehicle : garage) {
            vehicle.start();
            vehicle.accelerate(100);
            System.out.println(vehicle.getInfo()); // каждый вызовет СВОЙ переопределенный метод
            System.out.println();
        }
        
        System.out.println("═══════════════════════════════════════════════════════════\n");
    }
    
    public static void main(String[] args) {
        System.out.println("\n");
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║                                                        ║");
        System.out.println("║           УРОК 2: НАСЛЕДОВАНИЕ                         ║");
        System.out.println("║                                                        ║");
        System.out.println("║      Второй принцип ООП - Переиспользование кода      ║");
        System.out.println("║                                                        ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        demonstrateInheritance();
        demonstrateTypeCasting();
        demonstratePolymorphismPreview();
        
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║                  КЛЮЧЕВЫЕ ВЫВОДЫ                      ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        System.out.println("✅ 1. extends - ключевое слово для наследования");
        System.out.println("✅ 2. super() - вызов конструктора родителя (ОБЯЗАТЕЛЬНО первым!)");
        System.out.println("✅ 3. Дочерний класс получает ВСЕ публичные методы родителя");
        System.out.println("✅ 4. @Override - переопределение методов родителя");
        System.out.println("✅ 5. super.method() - вызов метода родителя из дочернего класса");
        System.out.println("✅ 6. Дочерний класс может добавлять свои методы и поля");
        System.out.println("✅ 7. Наследование = отношение 'IS-A' (Car IS-A Vehicle)");
        System.out.println("✅ 8. Наследование = переиспользование кода + иерархия");
        
        System.out.println("\n" + "═".repeat(60));
        System.out.println("🎯 ТЫ ИЗУЧИЛ НАСЛЕДОВАНИЕ!");
        System.out.println("📚 Теперь переходи к практическим заданиям ниже");
        System.out.println("═".repeat(60) + "\n");
    }
}

/**
 * ============================================================================
 * ЧАСТЬ 8: ПРАКТИЧЕСКИЕ ЗАДАНИЯ
 * ============================================================================
 * 
 * ТЕПЕРЬ ТВОЯ ОЧЕРЕДЬ ПРАКТИКОВАТЬСЯ!
 * 
 * Прочитай теорию выше, изучи примеры кода, и выполни задания ниже.
 * 
 * ⚠️ ВАЖНО: Пиши код САМОСТОЯТЕЛЬНО с нуля!
 * Не копируй из примеров - переосмысли и напиши по-своему!
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 1: БАЗОВОЕ - ЖИВОТНЫЕ
 * ----------------------------------------------------------------------------
 * 
 * Создай иерархию классов для животных:
 * 
 * 1. Родительский класс Animal:
 *    - Поля: name (String), age (int), weight (double)
 *    - Конструктор: принимает name, age, weight
 *    - Методы:
 *      * eat() - выводит "Животное ест"
 *      * sleep() - выводит "Животное спит"
 *      * makeSound() - выводит "Животное издает звук" (можно переопределить)
 *      * getInfo() - возвращает строку с информацией
 * 
 * 2. Дочерний класс Cat extends Animal:
 *    - Добавляет поле: breed (String) - порода
 *    - Конструктор: принимает все параметры + breed
 *    - Переопределяет makeSound() - выводит "Мяу!"
 *    - Уникальный метод: knockThingsOffTable() - выводит "Кот скинул что-то со стола"
 * 
 * 3. Дочерний класс Dog extends Animal:
 *    - Добавляет поле: breed (String) - порода
 *    - Конструктор: принимает все параметры + breed
 *    - Переопределяет makeSound() - выводит "Гав-гав!"
 *    - Уникальный метод: fetchBall() - выводит "Собака принесла мяч"
 * 
 * 4. Класс AnimalDemo с main для тестирования:
 *    - Создай несколько животных
 *    - Продемонстрируй наследование
 *    - Продемонстрируй приведение типов
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 2: СРЕДНЕЕ - СОТРУДНИКИ
 * ----------------------------------------------------------------------------
 * 
 * Создай иерархию классов для сотрудников компании:
 * 
 * 1. Родительский класс Employee:
 *    - Поля: name, id, salary (double)
 *    - Конструктор с параметрами
 *    - Методы:
 *      * work() - выводит "Сотрудник работает"
 *      * calculateSalary() - возвращает salary
 *      * getInfo() - возвращает информацию о сотруднике
 * 
 * 2. Дочерний класс Developer extends Employee:
 *    - Добавляет поле: programmingLanguage (String)
 *    - Переопределяет work() - выводит "Пишет код на " + language
 *    - Переопределяет calculateSalary() - возвращает salary * 1.2 (бонус)
 *    - Уникальный метод: debugCode() - выводит "Отлаживает код"
 * 
 * 3. Дочерний класс Manager extends Employee:
 *    - Добавляет поле: teamSize (int)
 *    - Переопределяет work() - выводит "Управляет командой из " + teamSize
 *    - Переопределяет calculateSalary() - возвращает salary * 1.5 (бонус)
 *    - Уникальный метод: conductMeeting() - выводит "Проводит встречу"
 * 
 * 4. Класс CompanyDemo с main для тестирования
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 3: ПРОДВИНУТОЕ - БИБЛИОТЕКА МЕДИА
 * ----------------------------------------------------------------------------
 * 
 * Создай иерархию для медиа-контента:
 * 
 * 1. Родительский класс Media:
 *    - Поля: title, year, duration (int - минуты)
 *    - Конструктор с параметрами
 *    - Методы:
 *      * play() - выводит "Воспроизводится " + title
 *      * getInfo() - возвращает информацию
 * 
 * 2. Дочерний класс Movie extends Media:
 *    - Добавляет поле: director (String)
 *    - Переопределяет play() - выводит подробную информацию о фильме
 *    - Уникальный метод: getRating() - возвращает рейтинг (придумай логику)
 * 
 * 3. Дочерний класс Music extends Media:
 *    - Добавляет поле: artist (String)
 *    - Переопределяет play() - выводит подробную информацию о треке
 *    - Уникальный метод: getGenre() - возвращает жанр (придумай логику)
 * 
 * 4. Дочерний класс Book extends Media:
 *    - Добавляет поле: author (String), pageCount (int)
 *    - Переопределяет play() - выводит "Читается книга " + title
 *    - Уникальный метод: readPage(int page) - выводит информацию о странице
 * 
 * 5. Класс MediaLibrary с массивом Media для демонстрации полиморфизма
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 4: МАСТЕРСКОЕ - ГЕОМЕТРИЧЕСКИЕ ФИГУРЫ
 * ----------------------------------------------------------------------------
 * 
 * Создай иерархию для геометрических фигур:
 * 
 * 1. Родительский класс Shape:
 *    - Поля: color (String)
 *    - Конструктор с параметрами
 *    - Абстрактные методы (мы их изучим в Уроке 4):
 *      * calculateArea() - абстрактный метод
 *      * calculatePerimeter() - абстрактный метод
 *    - Обычные методы:
 *      * draw() - выводит "Рисуется фигура цвета " + color
 *      * getInfo() - возвращает информацию
 * 
 * 2. Дочерний класс Circle extends Shape:
 *    - Добавляет поле: radius (double)
 *    - Реализует calculateArea() - Math.PI * radius * radius
 *    - Реализует calculatePerimeter() - 2 * Math.PI * radius
 *    - Переопределяет draw() - выводит "Рисуется круг"
 * 
 * 3. Дочерний класс Rectangle extends Shape:
 *    - Добавляет поля: width, height (double)
 *    - Реализует calculateArea() - width * height
 *    - Реализует calculatePerimeter() - 2 * (width + height)
 *    - Переопределяет draw() - выводит "Рисуется прямоугольник"
 * 
 * 4. Дочерний класс Triangle extends Shape:
 *    - Добавляет поля: side1, side2, side3 (double)
 *    - Реализует calculateArea() - формула Герона
 *    - Реализует calculatePerimeter() - side1 + side2 + side3
 *    - Переопределяет draw() - выводит "Рисуется треугольник"
 * 
 * 5. Класс ShapeDemo для тестирования всех фигур
 * 
 * ============================================================================
 * СОВЕТЫ ПО ВЫПОЛНЕНИЮ:
 * ============================================================================
 * 
 * 1. Начни с родительского класса
 * 2. Определи общие поля и методы
 * 3. Создай дочерний класс с extends
 * 4. Вызови super() в конструкторе ДОЧЕРНЕГО класса ПЕРВЫМ
 * 5. Добавь уникальные поля и методы
 * 6. Переопредели методы, если нужно (@Override)
 * 7. Протестируй каждый класс
 * 
 * ============================================================================
 * КРИТЕРИИ ОЦЕНКИ:
 * ============================================================================
 * 
 * ✅ Правильное использование extends
 * ✅ Вызов super() в конструкторе дочернего класса
 * ✅ Использование @Override для переопределения методов
 * ✅ Правильная иерархия классов
 * ✅ Уникальные методы и поля в дочерних классах
 * ✅ Код компилируется без ошибок
 * ✅ Логика работает правильно
 * 
 * ============================================================================
 * 
 * Удачи в выполнении заданий! 💪
 * После выполнения переходи к Уроку 3: Полиморфизм
 * 
 * ============================================================================
 */
