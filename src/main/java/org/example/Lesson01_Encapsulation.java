package org.example;

/**
 * ============================================================================
 * УРОК 1: ИНКАПСУЛЯЦИЯ (ENCAPSULATION)
 * ============================================================================
 * 
 * ПРИВЕТ, БУДУЩИЙ ПРОГРАММИСТ! 🚀
 * 
 * Сегодня ты изучишь ПЕРВЫЙ из четырех принципов объектно-ориентированного 
 * программирования - ИНКАПСУЛЯЦИЮ.
 * 
 * Это фундамент ООП, без которого невозможно двигаться дальше.
 * 
 * ============================================================================
 * ЧАСТЬ 1: ТЕОРИЯ - ЧТО ТАКОЕ ИНКАПСУЛЯЦИЯ?
 * ============================================================================
 * 
 * ИНКАПСУЛЯЦИЯ (Encapsulation) - это принцип сокрытия внутренних деталей
 * реализации объекта от внешнего мира и предоставление контролируемого 
 * доступа к данным.
 * 
 * ПРОСТЫМИ СЛОВАМИ:
 * 
 * Инкапсуляция = "Спрячь всё лишнее, покажи только нужное"
 * 
 * Это как твоя квартира:
 * - Ты не хочешь, чтобы любой прохожий мог открыть дверь и зайти (private)
 * - Вместо этого есть дверной звонок, ключ, домофон - контролируемый доступ (public методы)
 * - Внутри квартиры твоя приватная жизнь - никто не должен знать детали (скрытые поля)
 * 
 * ============================================================================
 * ЧАСТЬ 2: АНАЛОГИИ ИЗ ЖИЗНИ (ЧТОБЫ ЗАПОМНИЛОСЬ НАВСЕГДА)
 * ============================================================================
 * 
 * АНАЛОГИЯ 1: БАНКОМАТ
 * 
 * Ты не можешь напрямую менять баланс в банке - это было бы опасно!
 * Вместо этого есть интерфейс (экраны, кнопки) через который ты можешь:
 * - Посмотреть баланс (getBalance)
 * - Снять деньги (withdraw) - с проверками!
 * - Пополнить счет (deposit) - с проверками!
 * 
 * Внутренние детали (как хранятся данные, база данных) - скрыты от тебя.
 * 
 * АНАЛОГИЯ 2: ТЕЛЕВИЗОР
 * 
 * Ты не можешь напрямую менять транзисторы внутри телевизора (private поля).
 * Вместо этого есть пульт с кнопками (public методы):
 * - Включить (powerOn)
 * - Изменить канал (changeChannel)
 * - Увеличить громкость (increaseVolume)
 * 
 * Внутри телевизор может быть как угодно сложным - тебе это не важно.
 * 
 * АНАЛОГИЯ 3: ТВОЙ МОЗГ
 * 
 * Твой мозг инкапсулирован в черепе. Никто не может напрямую менять нейроны.
 * Вместо этого есть интерфейсы:
 * - Глаза (визуальная информация)
 * - Уши (звуковая информация)
 * - Рот (общение наружу)
 * 
 * Внутренняя работа мозга скрыта - и это правильно!
 * 
 * ============================================================================
 * ЧАСТЬ 3: КАК ЭТО РАБОТАЕТ В JAVA
 * ============================================================================
 * 
 * В Java инкапсуляция реализуется через:
 * 
 * 1. МОДИФИКАТОРЫ ДОСТУПА (Access Modifiers)
 * 
 *    private   - доступ ТОЛЬКО внутри класса (твоя квартира, закрытая дверь)
 *    public    - доступ откуда угодно (пульт от телевизора, публичный интерфейс)
 *    protected - доступ в классе и наследниках (семейный доступ)
 *    (default) - доступ в том же пакете (соседи в доме)
 * 
 * 2. GETTER И SETTER МЕТОДЫ
 * 
 *    getter - метод для ЧТЕНИЯ данных (getBalance)
 *    setter - метод для ЗАПИСИ данных (setBalance) - с проверками!
 * 
 * 3. БИЗНЕС-МЕТОДЫ
 * 
 *    Методы, которые выполняют бизнес-логику (deposit, withdraw)
 *    вместо прямого изменения полей
 * 
 * ============================================================================
 * ЧАСТЬ 4: ЗАЧЕМ ЭТО НУЖНО? (РЕАЛЬНЫЕ ПРИЧИНЫ)
 * ============================================================================
 * 
 * 1. ЗАЩИТА ДАННЫХ
 *    Без инкапсуляции кто угодно может сделать:
 *    account.balance = -999999999; // Баланс стал отрицательным - БАГ!
 *    
 *    С инкапсуляцией:
 *    account.setBalance(-999999999); // Проверка не пропустит!
 * 
 * 2. ГИБКОСТЬ
 *    Можешь менять внутреннюю реализацию, не ломая внешний код.
 *    Пользователь работает через методы - ему всё равно, как внутри.
 * 
 * 3. УПРОЩЕНИЕ
 *    Пользователю не нужно знать детали - он просто вызывает методы.
 *    Как пользователь телевизора - нажимает кнопки, не думая о схеме.
 * 
 * 4. ВАЛИДАЦИЯ
 *    В setter'ах можно проверять данные перед записью.
 *    Гарантия, что данные всегда корректны.
 * 
 * ============================================================================
 * ЧАСТЬ 5: ПЛОХОЙ VS ХОРОШИЙ ПРИМЕР
 * ============================================================================
 */

// ============================================
// ПРИМЕР 1: ПЛОХОЙ КОД (БЕЗ ИНКАПСУЛЯЦИИ)
// ============================================
class BadBankAccount {
    // ПЛОХО: публичное поле - любой может изменить баланс напрямую!
    public double balance; // Опасно! Можно сделать balance = -999999999
    
    // Проблема: нет контроля над изменениями
    // Проблема: нет валидации
    // Проблема: нельзя логировать изменения
    // Проблема: нельзя изменить реализацию без ломания кода
    
    // Что может пойти не так:
    // 1. balance = -1000; // Отрицательный баланс
    // 2. balance = Double.MAX_VALUE; // Огромное значение
    // 3. balance = 0.0000000001; // Очень маленькое значение
    // 4. Никто не узнает, когда и кто изменил баланс
}

// ============================================
// ПРИМЕР 2: ХОРОШИЙ КОД (С ИНКАПСУЛЯЦИЕЙ)
// ============================================
class GoodBankAccount {
    // ХОРОШО: приватное поле - нельзя изменить напрямую
    // Это как сейф - данные защищены!
    private double balance;
    private String ownerName;
    
    // ============================================
    // КОНСТРУКТОР - инициализация при создании
    // ============================================
    public GoodBankAccount(String ownerName, double initialBalance) {
        // Валидация ПРИ создании объекта
        if (initialBalance < 0) {
            throw new IllegalArgumentException(
                "Баланс не может быть отрицательным! Попытка: " + initialBalance);
        }
        this.ownerName = ownerName;
        this.balance = initialBalance;
        
        System.out.println("Создан счет для " + ownerName + " с балансом: " + initialBalance);
    }
    
    // ============================================
    // GETTER - только для чтения
    // ============================================
    public double getBalance() {
        // Можно добавить проверку прав доступа
        // Можно логировать каждый запрос
        // Можно кешировать значение
        return balance;
    }
    
    public String getOwnerName() {
        return ownerName;
    }
    
    // ============================================
    // SETTER - контролируемое изменение
    // ============================================
    // МОЖНО добавить:
    // - Проверки (валидацию)
    // - Логирование
    // - Уведомления
    // - История изменений
    public void setBalance(double newBalance) {
        // ВАЛИДАЦИЯ: проверяем корректность
        if (newBalance < 0) {
            System.out.println("ОШИБКА: Баланс не может быть отрицательным! " +
                             "Попытка установить: " + newBalance);
            return; // не меняем
        }
        
        // ЛОГИРОВАНИЕ: записываем изменение
        System.out.println("Изменение баланса с " + balance + " на " + newBalance);
        
        // УВЕДОМЛЕНИЕ: можно отправить email владельцу
        if (newBalance < balance) {
            System.out.println("ВНИМАНИЕ: Баланс уменьшился!");
        }
        
        // ТОЛЬКО ПОСЛЕ ВСЕХ ПРОВЕРОК изменяем значение
        this.balance = newBalance;
    }
    
    // ============================================
    // БИЗНЕС-МЕТОДЫ - правильные способы изменения
    // ============================================
    // Это методы, которые отражают реальные действия пользователя
    
    /**
     * Пополнение счета
     * Это правильный способ изменить баланс - через бизнес-метод
     */
    public void deposit(double amount) {
        // Проверка: можно положить только положительную сумму
        if (amount <= 0) {
            throw new IllegalArgumentException(
                "Нельзя положить 0 или меньше! Попытка: " + amount);
        }
        
        // Изменяем баланс
        balance += amount;
        
        // Логируем операцию
        System.out.println("✅ Пополнение: +" + amount + 
                          " руб. Новый баланс: " + balance);
        
        // Можно добавить:
        // - Запись в базу данных
        // - Отправку уведомления
        // - Обновление статистики
    }
    
    /**
     * Снятие денег со счета
     * Еще один правильный способ изменения баланса
     */
    public boolean withdraw(double amount) {
        // Проверка 1: сумма должна быть положительной
        if (amount <= 0) {
            System.out.println("❌ Ошибка: Нельзя снять 0 или меньше!");
            return false;
        }
        
        // Проверка 2: достаточно ли средств
        if (balance < amount) {
            System.out.println("❌ Ошибка: Недостаточно средств! " +
                             "Текущий баланс: " + balance + 
                             ", запрошено: " + amount);
            return false;
        }
        
        // Все проверки пройдены - снимаем деньги
        balance -= amount;
        System.out.println("✅ Снятие: -" + amount + 
                          " руб. Остаток: " + balance);
        
        return true; // операция успешна
    }
    
    // ============================================
    // ПРИВАТНЫЙ МЕТОД - только для внутреннего использования
    // ============================================
    // Пользователь класса не может вызвать этот метод напрямую
    // Используется внутри класса для вспомогательных операций
    @SuppressWarnings("unused")
    private void logTransaction(String action, double amount) {
        // В реальном приложении здесь была бы логика:
        // - Запись в файл логов
        // - Отправка в систему мониторинга
        // - Сохранение в базу данных
        System.out.println("[ЛОГ] Транзакция: " + action + ", сумма: " + amount);
    }
}

/**
 * ============================================================================
 * ЧАСТЬ 6: ДЕМОНСТРАЦИЯ - ВИДИМ РАЗНИЦУ
 * ============================================================================
 */
public class Lesson01_Encapsulation {
    
    /**
     * Демонстрация ПЛОХОГО подхода (без инкапсуляции)
     */
    public static void demonstrateBadCode() {
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║  ПЛОХОЙ ПРИМЕР: БЕЗ ИНКАПСУЛЯЦИИ                       ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        BadBankAccount badAccount = new BadBankAccount();
        badAccount.balance = 1000.0; // можно напрямую
        
        System.out.println("Начальный баланс: " + badAccount.balance);
        
        // ПРОБЛЕМА 1: можно сделать что угодно!
        badAccount.balance = -999999999.0; 
        System.out.println("После прямой установки: " + badAccount.balance);
        System.out.println("❌ Баланс стал отрицательным - БАГ!\n");
        
        // ПРОБЛЕМА 2: нет проверок
        badAccount.balance = Double.MAX_VALUE; 
        System.out.println("После ещё одной установки: " + badAccount.balance);
        System.out.println("❌ Баланс стал огромным - БАГ!\n");
        
        // ПРОБЛЕМА 3: невозможно отследить, кто и когда изменил
        System.out.println("❌ Нельзя узнать, кто изменил баланс");
        System.out.println("❌ Нельзя добавить логирование");
        System.out.println("❌ Нельзя добавить валидацию\n");
        
        System.out.println("═══════════════════════════════════════════════════════════\n");
    }
    
    /**
     * Демонстрация ХОРОШЕГО подхода (с инкапсуляцией)
     */
    public static void demonstrateGoodCode() {
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║  ХОРОШИЙ ПРИМЕР: С ИНКАПСУЛЯЦИЕЙ                        ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        // Создаем счет с валидацией при создании
        GoodBankAccount goodAccount = new GoodBankAccount("Иван", 1000.0);
        System.out.println("Владелец: " + goodAccount.getOwnerName());
        System.out.println("Баланс: " + goodAccount.getBalance() + "\n");
        
        // Попытка установить отрицательный баланс через setter
        System.out.println("--- Попытка установить отрицательный баланс ---");
        goodAccount.setBalance(-500); // будет отклонено!
        System.out.println("Текущий баланс: " + goodAccount.getBalance() + "\n");
        
        // Правильное использование - через бизнес-методы
        System.out.println("--- Правильное использование через методы ---");
        goodAccount.deposit(500);     // Пополнение
        goodAccount.withdraw(200);    // Снятие (успешно)
        goodAccount.withdraw(2000);   // Снятие (недостаточно средств - отклонено)
        
        System.out.println("\nФинальный баланс: " + goodAccount.getBalance());
        System.out.println("✅ Защита данных работает!\n");
        
        System.out.println("═══════════════════════════════════════════════════════════\n");
    }
    
    /**
     * Демонстрация преимуществ инкапсуляции
     */
    public static void demonstrateBenefits() {
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║  ПРЕИМУЩЕСТВА ИНКАПСУЛЯЦИИ                              ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        GoodBankAccount account = new GoodBankAccount("Петр", 5000.0);
        
        System.out.println("1. ЗАЩИТА ДАННЫХ:");
        System.out.println("   - Прямой доступ к balance невозможен");
        System.out.println("   - account.balance = 1000; // ❌ ОШИБКА КОМПИЛЯЦИИ!\n");
        
        System.out.println("2. ВАЛИДАЦИЯ:");
        account.setBalance(-100); // Отклонено!
        System.out.println();
        
        System.out.println("3. ЛОГИРОВАНИЕ:");
        account.deposit(100); // Автоматически логируется
        System.out.println();
        
        System.out.println("4. ГИБКОСТЬ:");
        System.out.println("   Можно изменить внутреннюю реализацию,");
        System.out.println("   не меняя код, который использует класс\n");
        
        System.out.println("═══════════════════════════════════════════════════════════\n");
    }
    
    public static void main(String[] args) {
        System.out.println("\n");
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║                                                        ║");
        System.out.println("║           УРОК 1: ИНКАПСУЛЯЦИЯ                          ║");
        System.out.println("║                                                        ║");
        System.out.println("║      Первый принцип ООП - Фундамент знаний            ║");
        System.out.println("║                                                        ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        demonstrateBadCode();
        demonstrateGoodCode();
        demonstrateBenefits();
        
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║                  КЛЮЧЕВЫЕ ВЫВОДЫ                      ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        System.out.println("✅ 1. private поля защищают данные от прямого доступа");
        System.out.println("✅ 2. public методы дают контролируемый доступ");
        System.out.println("✅ 3. getter'ы - для чтения данных");
        System.out.println("✅ 4. setter'ы - для записи с валидацией");
        System.out.println("✅ 5. Бизнес-методы - правильный способ изменения состояния");
        System.out.println("✅ 6. Валидация предотвращает баги");
        System.out.println("✅ 7. Логирование помогает отслеживать изменения");
        System.out.println("✅ 8. Инкапсуляция = сокрытие деталей + контролируемый доступ");
        
        System.out.println("\n" + "═".repeat(60));
        System.out.println("🎯 ТЫ ИЗУЧИЛ ИНКАПСУЛЯЦИЮ!");
        System.out.println("📚 Теперь переходи к практическим заданиям ниже");
        System.out.println("═".repeat(60) + "\n");
    }
}

/**
 * ============================================================================
 * ЧАСТЬ 7: ПРАКТИЧЕСКИЕ ЗАДАНИЯ
 * ============================================================================
 * 
 * ТЕПЕРЬ ТВОЯ ОЧЕРЕДЬ ПРАКТИКОВАТЬСЯ!
 * 
 * Прочитай теорию выше, изучи примеры кода, и выполни задания ниже.
 * 
 * ⚠️ ВАЖНО: Пиши код САМОСТОЯТЕЛЬНО с нуля!
 * Не копируй из примеров - переосмысли и напиши по-своему!
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 1: БАЗОВОЕ - КЛАСС С ИНКАПСУЛЯЦИЕЙ
 * ----------------------------------------------------------------------------
 * 
 * Создай класс Student (Студент) с инкапсуляцией:
 * 
 * Требования:
 * 1. Поля (все private):
 *    - name (String) - имя студента
 *    - age (int) - возраст студента
 *    - gpa (double) - средний балл (от 0.0 до 4.0)
 * 
 * 2. Конструктор:
 *    - Принимает name, age, gpa
 *    - Валидирует: возраст от 16 до 100
 *    - Валидирует: GPA от 0.0 до 4.0
 *    - Если данные неверные - выбрасывает исключение
 * 
 * 3. Getter'ы для всех полей (getName, getAge, getGpa)
 * 
 * 4. Setter'ы с валидацией:
 *    - setAge(int age) - проверяет диапазон 16-100
 *    - setGpa(double gpa) - проверяет диапазон 0.0-4.0
 *    - Если данные неверные - выводит ошибку и НЕ меняет значение
 * 
 * 5. Бизнес-методы:
 *    - upgradeGpa(double points) - увеличивает GPA на points (с проверкой максимума)
 *    - celebrateBirthday() - увеличивает возраст на 1
 * 
 * Создай класс StudentDemo с main методом для тестирования.
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 2: СРЕДНЕЕ - БАНКОВСКИЙ СЧЕТ
 * ----------------------------------------------------------------------------
 * 
 * Создай класс BankAccount (Банковский счет) с полной инкапсуляцией:
 * 
 * Требования:
 * 1. Поля (все private):
 *    - accountNumber (String) - номер счета (только чтение, без setter)
 *    - balance (double) - баланс
 *    - ownerName (String) - имя владельца
 * 
 * 2. Конструктор:
 *    - Принимает accountNumber и ownerName
 *    - Устанавливает balance = 0.0
 * 
 * 3. Getter'ы для всех полей
 * 
 * 4. НЕТ setter для balance - баланс меняется ТОЛЬКО через методы
 * 
 * 5. Бизнес-методы:
 *    - deposit(double amount) - пополнение (amount > 0)
 *    - withdraw(double amount) - снятие (amount > 0, проверка баланса)
 *    - transfer(BankAccount other, double amount) - перевод на другой счет
 *    - getAccountInfo() - возвращает строку с информацией о счете
 * 
 * 6. Приватный метод:
 *    - validateAmount(double amount) - проверяет, что amount > 0
 * 
 * Создай класс BankDemo для тестирования.
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 3: ПРОДВИНУТОЕ - ТЕЛЕФОН
 * ----------------------------------------------------------------------------
 * 
 * Создай класс MobilePhone (Мобильный телефон) с инкапсуляцией:
 * 
 * Требования:
 * 1. Поля (все private):
 *    - brand (String) - марка телефона
 *    - batteryLevel (int) - уровень батареи (0-100)
 *    - isOn (boolean) - включен ли телефон
 * 
 * 2. Конструктор:
 *    - Принимает brand
 *    - batteryLevel = 100 (полный заряд)
 *    - isOn = false (выключен)
 * 
 * 3. Getter'ы для всех полей
 * 
 * 4. Бизнес-методы:
 *    - powerOn() - включает телефон (если батарея > 0)
 *    - powerOff() - выключает телефон
 *    - charge() - заряжает батарею до 100%
 *    - usePhone(int minutes) - использует телефон (уменьшает батарею)
 *      - Батарея уменьшается на minutes
 *      - Если батарея = 0, телефон выключается
 *    - makeCall(int minutes) - звонит (можно только если включен и батарея > 0)
 * 
 * 5. Приватные методы:
 *    - decreaseBattery(int amount) - уменьшает батарею
 *    - checkBattery() - проверяет, не разрядилась ли батарея
 * 
 * Создай класс PhoneDemo для тестирования.
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 4: МАСТЕРСКОЕ - БИБЛИОТЕКА
 * ----------------------------------------------------------------------------
 * 
 * Создай класс Library (Библиотека) для управления книгами:
 * 
 * Требования:
 * 1. Поля (все private):
 *    - books (List<Book>) - список книг
 *    - maxCapacity (int) - максимальная вместимость
 * 
 * 2. Внутренний класс Book:
 *    - title (String)
 *    - author (String)
 *    - isAvailable (boolean)
 * 
 * 3. Конструктор:
 *    - Принимает maxCapacity
 *    - Инициализирует пустой список книг
 * 
 * 4. Методы:
 *    - addBook(String title, String author) - добавляет книгу
 *      - Проверяет, не превышена ли вместимость
 *    - removeBook(String title) - удаляет книгу
 *    - borrowBook(String title) - берет книгу в аренду (меняет isAvailable)
 *    - returnBook(String title) - возвращает книгу
 *    - findAvailableBooks() - возвращает список доступных книг
 *    - getLibraryInfo() - возвращает информацию о библиотеке
 * 
 * Создай класс LibraryDemo для тестирования.
 * 
 * ============================================================================
 * СОВЕТЫ ПО ВЫПОЛНЕНИЮ:
 * ============================================================================
 * 
 * 1. Начни с простого - создай класс с полями
 * 2. Сделай все поля private
 * 3. Добавь конструктор с валидацией
 * 4. Добавь getter'ы для чтения
 * 5. Добавь setter'ы с валидацией
 * 6. Добавь бизнес-методы
 * 7. Тестируй каждый метод по отдельности
 * 
 * ============================================================================
 * КРИТЕРИИ ОЦЕНКИ:
 * ============================================================================
 * 
 * ✅ Все поля private
 * ✅ Есть конструктор с валидацией
 * ✅ Есть getter'ы
 * ✅ Есть setter'ы или бизнес-методы с валидацией
 * ✅ Код компилируется без ошибок
 * ✅ Логика работает правильно
 * 
 * ============================================================================
 * 
 * Удачи в выполнении заданий! 💪
 * После выполнения переходи к Уроку 2: Наследование
 * 
 * ============================================================================
 */
