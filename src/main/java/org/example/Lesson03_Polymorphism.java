package org.example;

/**
 * ============================================================================
 * УРОК 3: ПОЛИМОРФИЗМ (POLYMORPHISM)
 * ============================================================================
 * 
 * ПРИВЕТ, БУДУЩИЙ ПРОГРАММИСТ! 🚀
 * 
 * Ты уже изучил ИНКАПСУЛЯЦИЮ и НАСЛЕДОВАНИЕ - первые два принципа ООП.
 * Теперь изучаем третий принцип - ПОЛИМОРФИЗМ.
 * 
 * Это магия ООП - один код работает с разными типами!
 * 
 * ============================================================================
 * ЧАСТЬ 1: ТЕОРИЯ - ЧТО ТАКОЕ ПОЛИМОРФИЗМ?
 * ============================================================================
 * 
 * ПОЛИМОРФИЗМ (Polymorphism) - это способность объектов разных классов
 * обрабатываться через один и тот же интерфейс.
 * 
 * ПРОСТЫМИ СЛОВАМИ:
 * 
 * Полиморфизм = "один интерфейс, много реализаций"
 * 
 * Один метод (makeSound), разное поведение:
 * - Cat: "Мяу!"
 * - Dog: "Гав-гав!"
 * - Bird: "Чирик!"
 * 
 * Все животные, но звуки разные!
 * 
 * ============================================================================
 * ЧАСТЬ 2: АНАЛОГИИ ИЗ ЖИЗНИ (ЧТОБЫ ЗАПОМНИЛОСЬ НАВСЕГДА)
 * ============================================================================
 * 
 * АНАЛОГИЯ 1: КНОПКА "ВКЛЮЧИТЬ" НА РАЗНЫХ ПРИБОРАХ
 * 
 * У всех приборов есть кнопка "Включить":
 * - Телевизор - показывает картинку
 * - Кофеварка - начинает варить кофе
 * - Микроволновка - начинает греть
 * - Свет - загорается
 * 
 * ОДНА кнопка "Включить", но РАЗНОЕ поведение!
 * 
 * В коде это выглядит так:
 * device.turnOn() // каждый прибор включится по-своему!
 * 
 * АНАЛОГИЯ 2: ЗВУКИ ЖИВОТНЫХ
 * 
 * Все животные умеют издавать звук (один интерфейс),
 * но каждый делает это по-своему (много реализаций):
 * 
 * - КОТ: "Мяу!"
 * - СОБАКА: "Гав-гав!"
 * - ПТИЦА: "Чирик!"
 * - КОРОВА: "Му!"
 * - ОВЦА: "Бе!"
 * 
 * В коде:
 * Animal[] animals = {new Cat(), new Dog(), new Bird()};
 * for (Animal animal : animals) {
 *     animal.makeSound(); // каждый издаст СВОЙ звук!
 * }
 * 
 * АНАЛОГИЯ 3: ТРАНСПОРТ
 * 
 * Все транспортные средства умеют ехать:
 * - Машина - едет по дороге на колесах
 * - Самолет - летит по небу на крыльях
 * - Корабль - плывет по воде
 * - Велосипед - едет по дороге, крутя педали
 * 
 * Один метод move(), разная реализация!
 * 
 * ============================================================================
 * ЧАСТЬ 3: ВИДЫ ПОЛИМОРФИЗМА
 * ============================================================================
 * 
 * В Java есть ДВА вида полиморфизма:
 * 
 * 1. ПОЛИМОРФИЗМ ВРЕМЕНИ КОМПИЛЯЦИИ (Compile-time Polymorphism)
 *    = ПЕРЕГРУЗКА МЕТОДОВ (Method Overloading)
 * 
 *    Одно имя метода, РАЗНЫЕ параметры:
 *    
 *    int add(int a, int b) { return a + b; }
 *    double add(double a, double b) { return a + b; }
 *    int add(int a, int b, int c) { return a + b + c; }
 * 
 *    Компилятор ВО ВРЕМЯ КОМПИЛЯЦИИ определяет, какой метод вызвать
 *    по количеству и типам параметров.
 * 
 * 2. ПОЛИМОРФИЗМ ВРЕМЕНИ ВЫПОЛНЕНИЯ (Runtime Polymorphism)
 *    = ПЕРЕОПРЕДЕЛЕНИЕ МЕТОДОВ (Method Overriding)
 * 
 *    Один метод, РАЗНЫЕ реализации в дочерних классах:
 *    
 *    class Animal {
 *        void makeSound() { System.out.println("Звук"); }
 *    }
 *    
 *    class Cat extends Animal {
 *        @Override
 *        void makeSound() { System.out.println("Мяу!"); }
 *    }
 *    
 *    class Dog extends Animal {
 *        @Override
 *        void makeSound() { System.out.println("Гав!"); }
 *    }
 * 
 *    Во ВРЕМЯ ВЫПОЛНЕНИЯ определяется, какой метод вызвать
 *    в зависимости от РЕАЛЬНОГО типа объекта.
 * 
 * ============================================================================
 * ЧАСТЬ 4: КАК ЭТО РАБОТАЕТ В JAVA
 * ============================================================================
 * 
 * ПОЛИМОРФИЗМ ВРЕМЕНИ ВЫПОЛНЕНИЯ (Runtime Polymorphism):
 * 
 * class Animal {
 *     public void makeSound() {
 *         System.out.println("Животное издает звук");
 *     }
 * }
 * 
 * class Cat extends Animal {
 *     @Override
 *     public void makeSound() {
 *         System.out.println("Мяу!"); // своя реализация
 *     }
 * }
 * 
 * class Dog extends Animal {
 *     @Override
 *     public void makeSound() {
 *         System.out.println("Гав-гав!"); // своя реализация
 *     }
 * }
 * 
 * Использование:
 * Animal animal1 = new Cat();  // переменная типа Animal
 * Animal animal2 = new Dog();  // переменная типа Animal
 * 
 * animal1.makeSound(); // выведет "Мяу!" - вызвался метод Cat!
 * animal2.makeSound(); // выведет "Гав-гав!" - вызвался метод Dog!
 * 
 * ВО ВРЕМЯ ВЫПОЛНЕНИЯ определяется РЕАЛЬНЫЙ тип объекта
 * и вызывается соответствующий метод!
 * 
 * МАССИВЫ И ПОЛИМОРФИЗМ:
 * 
 * Animal[] animals = {
 *     new Cat(),
 *     new Dog(),
 *     new Bird()
 * };
 * 
 * for (Animal animal : animals) {
 *     animal.makeSound(); // каждый вызовет СВОЙ метод!
 * }
 * 
 * Один цикл обрабатывает ВСЕХ животных разного типа!
 * 
 * ============================================================================
 * ЧАСТЬ 5: ЗАЧЕМ ЭТО НУЖНО? (РЕАЛЬНЫЕ ПРЕИМУЩЕСТВА)
 * ============================================================================
 * 
 * 1. ГИБКОСТЬ КОДА
 * 
 *    БЕЗ полиморфизма (ПЛОХО):
 *    Animal[] animals = {new Cat(), new Dog(), new Bird()};
 *    for (Animal animal : animals) {
 *        if (animal instanceof Cat) {
 *            ((Cat) animal).makeSound();
 *        } else if (animal instanceof Dog) {
 *            ((Dog) animal).makeSound();
 *        } else if (animal instanceof Bird) {
 *            ((Bird) animal).makeSound();
 *        }
 *        // И так для каждого нового животного - КОШМАР!
 *    }
 * 
 *    С полиморфизмом (ХОРОШО):
 *    Animal[] animals = {new Cat(), new Dog(), new Bird()};
 *    for (Animal animal : animals) {
 *        animal.makeSound(); // ОДИН вызов, работает для ВСЕХ!
 *    }
 * 
 * 2. РАСШИРЯЕМОСТЬ
 * 
 *    Добавил новое животное? Ничего не нужно менять!
 *    class Duck extends Animal {
 *        @Override
 *        public void makeSound() { System.out.println("Кря-кря!"); }
 *    }
 * 
 *    animals = {new Cat(), new Dog(), new Bird(), new Duck()};
 *    for (Animal animal : animals) {
 *        animal.makeSound(); // Работает для Duck тоже!
 *    }
 * 
 * 3. УПРОЩЕНИЕ
 * 
 *    Один код работает с разными типами.
 *    Не нужно знать конкретный тип - достаточно знать интерфейс.
 * 
 * 4. СООТВЕТСТВИЕ ПРИНЦИПУ OPEN/CLOSED
 * 
 *    Код открыт для расширения (новые классы),
 *    но закрыт для изменения (не нужно менять существующий код).
 * 
 * ============================================================================
 * ЧАСТЬ 6: ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ (DYNAMIC DISPATCH)
 * ============================================================================
 * 
 * Динамический полиморфизм = метод вызывается в зависимости от
 * РЕАЛЬНОГО типа объекта, а не от типа переменной.
 * 
 * Animal animal = new Cat(); // переменная типа Animal
 * animal.makeSound();       // вызовет метод Cat, а не Animal!
 * 
 * Это происходит ВО ВРЕМЯ ВЫПОЛНЕНИЯ (runtime),
 * а не во время компиляции (compile-time).
 * 
 * ПОЧЕМУ ЭТО ВАЖНО:
 * 
 * Компилятор не знает, какой метод вызовется!
 * Это определяется во время выполнения программы.
 * 
 * Shape shape = new Circle(5);
 * shape.draw(); // вызовет Circle.draw(), а не Shape.draw()!
 * 
 * shape = new Rectangle(4, 6);
 * shape.draw(); // вызовет Rectangle.draw(), а не Shape.draw()!
 */

// ============================================
// ПРИМЕР 1: ПЕРЕОПРЕДЕЛЕНИЕ МЕТОДОВ
// ============================================
class Shape {
    public void draw() {
        System.out.println("Рисую фигуру...");
    }
    
    public double calculateArea() {
        return 0;
    }
}

class Circle extends Shape {
    private double radius;
    
    public Circle(double radius) {
        this.radius = radius;
    }
    
    @Override
    public void draw() {
        System.out.println("Рисую круг радиусом " + radius);
    }
    
    @Override
    public double calculateArea() {
        return Math.PI * radius * radius;
    }
}

class Rectangle extends Shape {
    private double width;
    private double height;
    
    public Rectangle(double width, double height) {
        this.width = width;
        this.height = height;
    }
    
    @Override
    public void draw() {
        System.out.println("Рисую прямоугольник " + width + "x" + height);
    }
    
    @Override
    public double calculateArea() {
        return width * height;
    }
}

// ============================================
// ПРИМЕР 2: ПЕРЕГРУЗКА МЕТОДОВ (OVERLOADING)
// ============================================
class Calculator {
    // Полиморфизм времени компиляции - разные сигнатуры
    
    public int add(int a, int b) {
        System.out.println("Складываю два int: " + a + " + " + b);
        return a + b;
    }
    
    public double add(double a, double b) {
        System.out.println("Складываю два double: " + a + " + " + b);
        return a + b;
    }
    
    public int add(int a, int b, int c) {
        System.out.println("Складываю три int: " + a + " + " + b + " + " + c);
        return a + b + c;
    }
    
    // Одинаковое имя, разные параметры = перегрузка!
}

/**
 * ДЕМОНСТРАЦИЯ ПОЛИМОРФИЗМА
 */
public class Lesson03_Polymorphism {
    
    public static void demonstratePolymorphism() {
        System.out.println("=== ПОЛИМОРФИЗМ В ДЕЙСТВИИ ===\n");
        
        // Создаем массив Shape, но заполняем разными типами!
        Shape[] shapes = {
            new Circle(5),
            new Rectangle(4, 6),
            new Circle(10)
        };
        
        System.out.println("--- ОДИН ЦИКЛ, РАЗНОЕ ПОВЕДЕНИЕ ---");
        // Полиморфизм: вызываем draw() для всех фигур
        // Каждая фигура нарисуется ПО-СВОЕМУ!
        for (Shape shape : shapes) {
            shape.draw(); // ПОЛИМОРФИЗМ! Один метод, разные реализации
            System.out.println("Площадь: " + shape.calculateArea());
            System.out.println();
        }
        
        System.out.println("--- ПОЛИМОРФИЗМ С ЖИВОТНЫМИ ---");
        Animal[] animals = {
            new Cat(),
            new Dog(),
            new Bird()
        };
        
        // Один цикл обрабатывает всех животных!
        for (Animal animal : animals) {
            animal.makeSound(); // каждое животное издаст СВОЙ звук!
            animal.play();      // каждое животное играет ПО-СВОЕМУ!
        }
    }
    
    public static void demonstrateMethodOverloading() {
        System.out.println("\n=== ПЕРЕГРУЗКА МЕТОДОВ (OVERLOADING) ===");
        
        Calculator calc = new Calculator();
        
        // Одно и то же имя метода, но разные параметры!
        int result1 = calc.add(5, 3);           // вызовет add(int, int)
        double result2 = calc.add(5.5, 3.2);    // вызовет add(double, double)
        int result3 = calc.add(1, 2, 3);        // вызовет add(int, int, int)
        
        System.out.println("\nРезультаты:");
        System.out.println("5 + 3 = " + result1);
        System.out.println("5.5 + 3.2 = " + result2);
        System.out.println("1 + 2 + 3 = " + result3);
        
        System.out.println("\nПерегрузка = одно имя, разные параметры!");
    }
    
    public static void demonstrateDynamicDispatch() {
        System.out.println("\n=== ДИНАМИЧЕСКИЙ ПОЛИМОРФИЗМ ===");
        
        // Переменная типа родителя, но объект дочернего класса
        Shape shape1 = new Circle(7);      // Shape ссылается на Circle
        Shape shape2 = new Rectangle(3, 4); // Shape ссылается на Rectangle
        
        // Во время выполнения определяется РЕАЛЬНЫЙ тип объекта!
        shape1.draw(); // вызовет Circle.draw(), а не Shape.draw()!
        shape2.draw(); // вызовет Rectangle.draw(), а не Shape.draw()!
        
        System.out.println("\nДинамический полиморфизм:");
        System.out.println("Компилятор не знает, какой метод вызовется!");
        System.out.println("Это определяется ВО ВРЕМЯ ВЫПОЛНЕНИЯ (runtime)!");
    }
    
    public static void demonstratePolymorphismBenefits() {
        System.out.println("\n=== ПРЕИМУЩЕСТВА ПОЛИМОРФИЗМА ===");
        
        // Представь: у тебя есть функция, которая работает с Animal
        // Без полиморфизма пришлось бы делать так:
        Animal[] zoo = {new Cat(), new Dog(), new Bird()};
        
        // ПЛОХОЙ КОД (без полиморфизма):
        /*
        for (Animal animal : zoo) {
            if (animal instanceof Cat) {
                ((Cat) animal).makeSound();
            } else if (animal instanceof Dog) {
                ((Dog) animal).makeSound();
            } else if (animal instanceof Bird) {
                ((Bird) animal).makeSound();
            }
            // И так для каждого нового животного - КОШМАР!
        }
        */
        
        // ХОРОШИЙ КОД (с полиморфизмом):
        System.out.println("--- ХОРОШИЙ КОД ---");
        for (Animal animal : zoo) {
            animal.makeSound(); // ОДИН вызов, работает для ВСЕХ!
        }
        
        System.out.println("\nПреимущества:");
        System.out.println("1. Код проще - один вызов вместо множества if");
        System.out.println("2. Расширяемость - добавил новое животное, ничего менять не нужно!");
        System.out.println("3. Гибкость - можно менять реализацию без изменения кода клиента");
    }
    
    public static void main(String[] args) {
        System.out.println("\n");
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║                                                        ║");
        System.out.println("║           УРОК 3: ПОЛИМОРФИЗМ                         ║");
        System.out.println("║                                                        ║");
        System.out.println("║      Третий принцип ООП - Один интерфейс,            ║");
        System.out.println("║                   много реализаций                  ║");
        System.out.println("║                                                        ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        demonstratePolymorphism();
        demonstrateMethodOverloading();
        demonstrateDynamicDispatch();
        demonstratePolymorphismBenefits();
        
        System.out.println("╔════════════════════════════════════════════════════════╗");
        System.out.println("║                  КЛЮЧЕВЫЕ ВЫВОДЫ                      ║");
        System.out.println("╚════════════════════════════════════════════════════════╝\n");
        
        System.out.println("✅ 1. Полиморфизм = один интерфейс, много реализаций");
        System.out.println("✅ 2. @Override - переопределение методов (runtime polymorphism)");
        System.out.println("✅ 3. Перегрузка методов - одно имя, разные параметры (compile time)");
        System.out.println("✅ 4. Динамический полиморфизм - метод вызывается по реальному типу объекта");
        System.out.println("✅ 5. Полиморфизм делает код гибким и расширяемым");
        System.out.println("✅ 6. Один цикл может обработать разные типы через общий интерфейс");
        System.out.println("✅ 7. Не нужно знать конкретный тип - достаточно знать интерфейс");
        System.out.println("✅ 8. Соответствие принципу Open/Closed");
        
        System.out.println("\n" + "═".repeat(60));
        System.out.println("🎯 ТЫ ИЗУЧИЛ ПОЛИМОРФИЗМ!");
        System.out.println("📚 Теперь переходи к практическим заданиям ниже");
        System.out.println("═".repeat(60) + "\n");
    }
}

/**
 * ============================================================================
 * ЧАСТЬ 7: ПРАКТИЧЕСКИЕ ЗАДАНИЯ
 * ============================================================================
 * 
 * ТЕПЕРЬ ТВОЯ ОЧЕРЕДЬ ПРАКТИКОВАТЬСЯ!
 * 
 * Прочитай теорию выше, изучи примеры кода, и выполни задания ниже.
 * 
 * ⚠️ ВАЖНО: Пиши код САМОСТОЯТЕЛЬНО с нуля!
 * Не копируй из примеров - переосмысли и напиши по-своему!
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 1: БАЗОВОЕ - ЖИВОТНЫЕ С ПОЛИМОРФИЗМОМ
 * ----------------------------------------------------------------------------
 * 
 * Создай иерархию животных с использованием полиморфизма:
 * 
 * 1. Родительский класс Animal:
 *    - Поля: name (String), age (int)
 *    - Конструктор с параметрами
 *    - Методы:
 *      * eat() - выводит "Животное ест"
 *      * sleep() - выводит "Животное спит"
 *      * makeSound() - выводит "Животное издает звук" (переопределяется)
 *      * getInfo() - возвращает строку с информацией
 * 
 * 2. Дочерний класс Cat extends Animal:
 *    - Переопределяет makeSound() - выводит "Мяу!"
 *    - Переопределяет eat() - выводит "Кот ест рыбу"
 *    - Уникальный метод: purr() - выводит "Мур-мур"
 * 
 * 3. Дочерний класс Dog extends Animal:
 *    - Переопределяет makeSound() - выводит "Гав-гав!"
 *    - Переопределяет eat() - выводит "Собака ест кость"
 *    - Уникальный метод: wagTail() - выводит "Виляет хвостом"
 * 
 * 4. Дочерний класс Bird extends Animal:
 *    - Переопределяет makeSound() - выводит "Чирик-чирик!"
 *    - Переопределяет eat() - выводит "Птица ест зерна"
 *    - Уникальный метод: fly() - выводит "Птица летит"
 * 
 * 5. Класс ZooDemo с main методом:
 *    - Создай массив Animal[] с разными животными
 *    - Один цикл обрабатывает всех животных (полиморфизм!)
 *    - Вызови makeSound() и eat() для каждого
 *    - Продемонстрируй динамический полиморфизм
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 2: СРЕДНЕЕ - ГЕОМЕТРИЧЕСКИЕ ФИГУРЫ
 * ----------------------------------------------------------------------------
 * 
 * Создай систему для работы с геометрическими фигурами:
 * 
 * 1. Родительский класс Shape:
 *    - Поле: color (String)
 *    - Конструктор с параметром color
 *    - Методы:
 *      * draw() - выводит "Рисую фигуру цвета " + color (переопределяется)
 *      * calculateArea() - возвращает 0 (переопределяется)
 *      * calculatePerimeter() - возвращает 0 (переопределяется)
 *      * getInfo() - возвращает информацию о фигуре
 * 
 * 2. Дочерний класс Circle extends Shape:
 *    - Поле: radius (double)
 *    - Переопределяет draw() - выводит "Рисую круг радиусом " + radius
 *    - Переопределяет calculateArea() - Math.PI * radius * radius
 *    - Переопределяет calculatePerimeter() - 2 * Math.PI * radius
 * 
 * 3. Дочерний класс Rectangle extends Shape:
 *    - Поля: width, height (double)
 *    - Переопределяет draw() - выводит "Рисую прямоугольник"
 *    - Переопределяет calculateArea() - width * height
 *    - Переопределяет calculatePerimeter() - 2 * (width + height)
 * 
 * 4. Дочерний класс Triangle extends Shape:
 *    - Поля: side1, side2, side3 (double)
 *    - Переопределяет draw() - выводит "Рисую треугольник"
 *    - Переопределяет calculateArea() - формула Герона
 *    - Переопределяет calculatePerimeter() - side1 + side2 + side3
 * 
 * 5. Класс ShapeGallery с main методом:
 *    - Создай массив Shape[] с разными фигурами
 *    - Один цикл обрабатывает все фигуры
 *    - Вызови draw(), calculateArea(), calculatePerimeter() для каждой
 *    - Продемонстрируй полиморфизм
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 3: ПРОДВИНУТОЕ - МЕДИА ПЛЕЕР
 * ----------------------------------------------------------------------------
 * 
 * Создай систему медиа-плеера с полиморфизмом:
 * 
 * 1. Родительский класс Media:
 *    - Поля: title, duration (int - минуты)
 *    - Методы:
 *      * play() - выводит "Воспроизводится " + title (переопределяется)
 *      * stop() - выводит "Остановлено" (переопределяется)
 *      * getInfo() - возвращает информацию
 * 
 * 2. Дочерний класс Music extends Media:
 *    - Поле: artist (String)
 *    - Переопределяет play() - выводит подробную информацию о треке
 *    - Переопределяет stop() - выводит "Музыка остановлена"
 *    - Уникальный метод: nextTrack() - переключает на следующий трек
 * 
 * 3. Дочерний класс Video extends Media:
 *    - Поле: director (String)
 *    - Переопределяет play() - выводит информацию о видео
 *    - Переопределяет stop() - выводит "Видео остановлено"
 *    - Уникальный метод: pause() - ставит на паузу
 * 
 * 4. Дочерний класс Podcast extends Media:
 *    - Поле: host (String)
 *    - Переопределяет play() - выводит информацию о подкасте
 *    - Переопределяет stop() - выводит "Подкаст остановлен"
 *    - Уникальный метод: skipAd() - пропускает рекламу
 * 
 * 5. Класс MediaPlayer с main методом:
 *    - Создай плейлист Media[] с разными медиа
 *    - Один цикл воспроизводит все медиа (полиморфизм!)
 *    - Вызови play() и stop() для каждого
 * 
 * ----------------------------------------------------------------------------
 * ЗАДАНИЕ 4: МАСТЕРСКОЕ - КАЛЬКУЛЯТОР С ПЕРЕГРУЗКОЙ
 * ----------------------------------------------------------------------------
 * 
 * Создай класс Calculator с перегрузкой методов:
 * 
 * 1. Метод add() с перегрузкой:
 *    - add(int a, int b) - складывает два int
 *    - add(double a, double b) - складывает два double
 *    - add(int a, int b, int c) - складывает три int
 *    - add(int[] numbers) - складывает массив int
 *    - add(double[] numbers) - складывает массив double
 * 
 * 2. Метод multiply() с перегрузкой:
 *    - multiply(int a, int b)
 *    - multiply(double a, double b)
 *    - multiply(int a, int b, int c)
 * 
 * 3. Метод calculate() с перегрузкой:
 *    - calculate(String operation, int a, int b)
 *    - calculate(String operation, double a, double b)
 *    - Поддержи операции: "add", "subtract", "multiply", "divide"
 * 
 * 4. Класс CalculatorDemo для тестирования всех вариантов
 * 
 * ============================================================================
 * СОВЕТЫ ПО ВЫПОЛНЕНИЮ:
 * ============================================================================
 * 
 * 1. Для полиморфизма времени выполнения:
 *    - Создай родительский класс с методом
 *    - Создай дочерние классы, переопределяющие этот метод (@Override)
 *    - Используй массив родительского типа для хранения разных объектов
 *    - Один цикл обрабатывает все объекты через общий метод
 * 
 * 2. Для перегрузки методов:
 *    - Одинаковое имя метода
 *    - Разные параметры (тип или количество)
 *    - Возвращаемый тип может быть разным
 * 
 * 3. Тестируй каждый метод отдельно
 * 4. Проверяй, что полиморфизм работает правильно
 * 5. Используй instanceof для проверки типа, если нужно
 * 
 * ============================================================================
 * КРИТЕРИИ ОЦЕНКИ:
 * ============================================================================
 * 
 * ✅ Правильное использование @Override
 * ✅ Массивы родительского типа для полиморфизма
 * ✅ Один цикл обрабатывает разные типы
 * ✅ Правильная перегрузка методов (разные параметры)
 * ✅ Динамический полиморфизм работает корректно
 * ✅ Код компилируется без ошибок
 * ✅ Логика работает правильно
 * 
 * ============================================================================
 * 
 * Удачи в выполнении заданий! 💪
 * После выполнения переходи к Уроку 4: Абстракция
 * 
 * ============================================================================
 */

